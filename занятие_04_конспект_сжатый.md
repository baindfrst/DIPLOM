# Занятие 4. Субградиентный метод и адаптивные методы оптимизации

## 1. Мотивация

Задача оптимизации: $\min_{x \in \mathbb{R}^d} f(x)$.

Градиентный спуск: $x_{k+1} = x_k - \gamma_k \nabla f(x_k)$, $\gamma_k > 0$.

Для гладких $L$-гладких выпуклых функций GD сходится со скоростью $O(1/K)$; для $\mu$-сильно выпуклых — $O((1-\mu\gamma)^K)$.

Функции вида $|x|$, $\|x\|_1$, $\|Ax\|_1$, $\lambda_{\max}(X)$, $\max(0,x)$ не являются $L$-гладкими: в «угловых» точках градиент не определён. Для $|x|$ при $y=\delta$, $x=-\delta$, $\delta>0$ из условия $L$-гладкости следует $L \geq 1/\delta \to +\infty$ при $\delta \to 0^+$.

Теоретический шаг GD $\gamma \sim 1/L$ требует конечной константы $L$; для негладких функций классическая теория GD неприменима напрямую.

---

## 2. $M$-липшицевость

**Определение.** Функция $f: \mathbb{R}^d \to \mathbb{R}$ — $M$-липшицева, если
$$|f(y) - f(x)| \leq M \|y - x\| \quad \forall x, y \in \mathbb{R}^d,$$
где $M > 0$ — константа Липшица.

$M$-липшицевая функция не может быть $\mu$-сильно выпуклой на всём $\mathbb{R}^d$: параболический рост $\sim \|y-x\|^2$ несовместим с линейной мажорантой $|f(y)-f(x)| \leq M\|y-x\|$.

---

## 3. Субградиент и субдифференциал

**Определение.** Вектор $g \in \mathbb{R}^d$ — субградиент выпуклой $f$ в точке $x$, если
$$f(y) \geq f(x) + \langle g, y - x \rangle \quad \forall y \in \mathbb{R}^d.$$

**Субдифференциал:** $\partial f(x) = \{g \in \mathbb{R}^d : g \text{ — субградиент } f \text{ в } x\}$.

**Условие оптимальности (выпуклая $f$):**
$$0 \in \partial f(x) \iff x \text{ — точка глобального минимума.}$$

**Теорема.** Пусть $f: \mathbb{R}^d \to \mathbb{R}$ выпукла. Тогда эквивалентно:
$$f \text{ — } M\text{-липшицева} \iff \forall x \in \mathbb{R}^d,\; \forall g \in \partial f(x): \|g\| \leq M.$$

---

## 4. Субградиентный метод

**Алгоритм:**
$$x_{k+1} = x_k - \gamma_k g_k, \quad g_k \in \partial f(x_k).$$

На каждом шаге допускается произвольный выбор элемента субдифференциала.

**Предположения для анализа сходимости:** $f$ выпуклая, $M$-липшицева; $x^* \in \arg\min f$; $R_0 = \|x_0 - x^*\|$; $\|g_k\| \leq M$.

**Оценка одной итерации.** Из раскрытия $\|x_{k+1} - x^*\|^2$ и субградиентного неравенства:
$$\|x_{k+1} - x^*\|^2 \leq \|x_k - x^*\|^2 - 2\gamma_k (f(x_k) - f^*) + \gamma_k^2 M^2.$$

**Телескопическая сумма** при постоянном шаге $\gamma_k = \gamma$:
$$\frac{1}{K}\sum_{k=0}^{K-1} (f(x_k) - f^*) \leq \frac{R_0^2}{2\gamma K} + \frac{\gamma M^2}{2}.$$

**Неравенство Йенсена.** Для выпуклой $f$ и весов $\alpha_k = 1/K$:
$$f\!\left(\frac{1}{K}\sum_{k=0}^{K-1} x_k\right) \leq \frac{1}{K}\sum_{k=0}^{K-1} f(x_k).$$

**Средняя итерация:** $\bar{x}_K = \frac{1}{K}\sum_{k=0}^{K-1} x_k$.

**Оценка недостатка:**
$$f(\bar{x}_K) - f^* \leq \frac{R_0^2}{2\gamma K} + \frac{\gamma M^2}{2} = \varphi(\gamma).$$

**Оптимальный постоянный шаг:** $\gamma^* = \dfrac{R_0}{M\sqrt{K}}$.

**Скорость сходимости:** $f(\bar{x}_K) - f^* = O\!\left(\dfrac{M R_0}{\sqrt{K}}\right) = O(1/\sqrt{K})$.

**Убывающий шаг:** $\gamma_k = \dfrac{R_0}{M\sqrt{k+1}}$ даёт ту же скорость $O(1/\sqrt{K})$ по среднему недостатку.

**Сравнение с GD:** для гладких выпуклых функций GD даёт $O(1/K)$ по последней точке; субградиентный метод требует усреднения $\bar{x}_K$ и сходится медленнее.

---

## 5. Адаптивные методы

Теоретический шаг $\gamma_k = R_0/(M\sqrt{k+1})$ требует знания $M$ и $R_0$. На практике оба параметра неизвестны.

**Эвристика:** замена $M\sqrt{k+1}$ на $\sqrt{\sum_{t=0}^{k} \|g_t\|^2}$. Для устранения деления на ноль добавляется $b > 0$.

### AdaGrad-Norm (Streeter, 2010; Duchi et al., 2011)
$$b_k^2 = b_{k-1}^2 + \|g_k\|^2, \quad b_{-1} = 0, \quad b_k > 0;$$
$$x_{k+1} = x_k - \frac{R_0}{b_k}\, g_k.$$

Скалярная адаптация шага; знаменатель монотонно растёт.

### Покоординатный AdaGrad
$$b_{k,i}^2 = b_{k-1,i}^2 + g_{k,i}^2; \qquad x_{k+1,i} = x_{k,i} - \frac{R_0}{b_{k,i}}\, g_{k,i}.$$

Каждая координата имеет собственный знаменатель. Недостаток: $b_{k,i}$ только растёт, шаги затухают на длинных траекториях.

### RMSProp-Norm (Tieleman, 2012)
$$v_k^2 = \beta_2 v_{k-1}^2 + (1-\beta_2)\|g_k\|^2, \quad \beta_2 \in (0,1);$$
$$x_{k+1} = x_k - \frac{D}{v_k}\, g_k.$$

Экспоненциальное сглаживание вместо полной суммы; знаменатель не растёт бесконечно.

### Adam-Norm (Kingma & Ba, 2014)
$$m_k = \beta_1 m_{k-1} + (1-\beta_1) g_k, \quad m_{-1} = 0;$$
$$v_k^2 = \beta_2 v_{k-1}^2 + (1-\beta_2)\|g_k\|^2;$$
$$x_{k+1} = x_k - \frac{D}{v_k}\, m_k.$$

При $\beta_1 = 0$ вырождается в RMSProp. Типичные значения: $\beta_1 = 0.9$, $\beta_2 = 0.999$.

### AMSGrad (Reddi et al., 2018)
$$\hat{v}_k^2 = \beta_2 v_{k-1}^2 + (1-\beta_2)\|g_k\|^2;$$
$$v_k^2 = \max(v_{k-1}^2, \hat{v}_k^2).$$

Принудительная монотонность $v_k^2$; устраняет возможность расходимости Adam при $\beta_2 < 1$ на стохастических выпуклых задачах.

### AdamW (Loshchilov & Hutter, 2017)
$$x_{k+1} = (1-\lambda_k)\, x_k - \frac{D}{v_k}\, m_k.$$

Развязанный weight decay: регуляризация $\|x\|_2^2$ применяется отдельно от адаптивного шага Adam.

---

## 6. Дополнительные направления

**NAdam** — комбинация Nesterov-ускорения с Adam; момент вычисляется с экстраполяцией.

**Sophia (2023)** — стохастическая оптимизация с диагональной оценкой гессиана для адаптивного масштабирования шага.

**D-Adaptation (2022)** — parameter-free метод; автоматическая оценка $R_0$ без ручного выбора масштаба шага.

**Robbins–Monro (1951)** — условия на шаг в стохастическом GD:
$$\sum_{k=0}^{\infty} \gamma_k = +\infty, \qquad \sum_{k=0}^{\infty} \gamma_k^2 < +\infty.$$
Шаг $\gamma_k = 1/k$ удовлетворяет обоим условиям; $\gamma_k = 1/\sqrt{k}$ — нет.

---

## 7. Ключевые формулы

- Субградиент: $f(y) \geq f(x) + \langle g, y - x \rangle$
- Оптимальность: $0 \in \partial f(x) \iff x$ — глобальный минимум
- $M$-липшицевость: $|f(y) - f(x)| \leq M \|y - x\|$
- Эквивалентность: $f$ $M$-липшицева $\iff \|g\| \leq M\;\forall g \in \partial f(x)$
- Субградиентный шаг: $x_{k+1} = x_k - \gamma_k g_k$, $g_k \in \partial f(x_k)$
- Средняя итерация: $\bar{x}_K = \frac{1}{K}\sum_{k=0}^{K-1} x_k$
- Оценка недостатка: $f(\bar{x}_K) - f^* \leq \dfrac{R_0^2}{2\gamma K} + \dfrac{\gamma M^2}{2}$
- Оптимальный шаг: $\gamma^* = \dfrac{R_0}{M\sqrt{K}}$
- Скорость: $O(M R_0 / \sqrt{K})$
- Убывающий шаг: $\gamma_k = \dfrac{R_0}{M\sqrt{k+1}}$
- AdaGrad-Norm: $b_k^2 = b_{k-1}^2 + \|g_k\|^2$; $x_{k+1} = x_k - (R_0/b_k)\,g_k$
- Покоординатный AdaGrad: $b_{k,i}^2 = b_{k-1,i}^2 + g_{k,i}^2$
- RMSProp-Norm: $v_k^2 = \beta_2 v_{k-1}^2 + (1-\beta_2)\|g_k\|^2$
- Adam: $m_k = \beta_1 m_{k-1} + (1-\beta_1)g_k$; $v_k^2 = \beta_2 v_{k-1}^2 + (1-\beta_2)\|g_k\|^2$
- AMSGrad: $v_k^2 = \max(v_{k-1}^2,\; \beta_2 v_{k-1}^2 + (1-\beta_2)\|g_k\|^2)$
- AdamW: $x_{k+1} = (1-\lambda_k)x_k - (D/v_k)\,m_k$
- Robbins–Monro: $\sum \gamma_k = \infty$; $\sum \gamma_k^2 < \infty$
